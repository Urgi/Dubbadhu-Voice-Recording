/**
 * Learner Home continue-card frame — keep aspect in sync with ContinueLessonCard (~520×304).
 */
export const HOME_CONTINUE_CARD_ASPECT_WIDTH = 520
export const HOME_CONTINUE_CARD_ASPECT_HEIGHT = 304

/** Upload / crop output (2× card pixels). */
export const HOME_CONTINUE_CARD_OUTPUT_WIDTH = 1040
export const HOME_CONTINUE_CARD_OUTPUT_HEIGHT = 608

/** Left edge of visible photo at top — from HERO_SWEEP_CLIP_PATH_OBJECT M0.284,0 */
export const HOME_SWEEP_HOLE_MIN_X_RATIO = 0.284

export const HOME_CONTINUE_CARD_ASPECT: [number, number] = [
  HOME_CONTINUE_CARD_ASPECT_WIDTH,
  HOME_CONTINUE_CARD_ASPECT_HEIGHT,
]
