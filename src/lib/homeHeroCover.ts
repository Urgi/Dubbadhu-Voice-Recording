/**
 * Learner Home continue-card frame — keep in sync with ContinueLessonCard
 * (phone-width × 304 so crop matches the tall Home card, not a short banner).
 */
export const HOME_CONTINUE_CARD_ASPECT_WIDTH = 390
export const HOME_CONTINUE_CARD_ASPECT_HEIGHT = 304

/** Upload / crop output (2× card pixels). */
export const HOME_CONTINUE_CARD_OUTPUT_WIDTH = 780
export const HOME_CONTINUE_CARD_OUTPUT_HEIGHT = 608

export const HOME_CONTINUE_CARD_ASPECT: [number, number] = [
  HOME_CONTINUE_CARD_ASPECT_WIDTH,
  HOME_CONTINUE_CARD_ASPECT_HEIGHT,
]
